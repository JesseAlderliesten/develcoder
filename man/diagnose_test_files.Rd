% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose_test_files.R
\name{diagnose_test_files}
\alias{diagnose_test_files}
\title{Diagnose test files}
\usage{
diagnose_test_files(
  path = fs::path_wd("inst", "tinytest"),
  pattern = "^test_|^test-",
  ignore_case = TRUE
)
}
\arguments{
\item{path}{\link[checkinput:is_character]{character string} containing a
\link[checkinput:is_path]{valid path} to the directory where to look for tests.}

\item{pattern}{\link[checkinput:is_character]{character string} containing a
\link[=regex]{regular expression} used to select test files.}

\item{ignore_case}{\code{TRUE} or \code{FALSE}: ignore case when matching filenames to
\code{pattern}?}
}
\value{
A \link{list} with six elements:
\itemize{
\item \code{pattern}: the value of argument \code{pattern}
\item \code{ignore_case}: the value of argument \code{ignore_case}
\item \code{status_testdir}: character string: \code{"fine"} if the test directory
indicated by \code{path} is present, otherwise \code{"missing"}
\item \code{status_test_files}: character string indicating the status of the test
files: \code{"fine"} if test files are present; \code{"wrong"} if files are ignored;
\code{"missing"} if no test files are present
\item \code{test_files}: character vector with the names of test files. \code{character(0)}
if no test files are present.
\item \code{ignored_files}: character vector with the names of files in \code{path} that
are \strong{not} test files. \code{character(0)} if no files are ignored.
}
}
\description{
Get test files, with an overview of various diagnostics
}
\details{
The default \code{path} points to directory \code{inst/tinytest} of the current
\link[=getwd]{working directory}, where
\href{https://CRAN.R-project.org/package=tinytest}{\code{tinytest}} stores tests when a
package is in development.

A warning is issued if:
\itemize{
\item \code{path} contains files that are ignored because their names do \strong{not} start
with pattern \code{pattern}, are not \R files, or are template files created by
\href{https://CRAN.R-project.org/package=tinytest}{\code{tinytest}}
}

\code{diagnose_test_files()} does \strong{not} warn about other problems detected with
the test files: that is deferred to \code{\link[=check_test_files]{check_test_files()}}.
}
\examples{
path_develcoder <- find.package("develcoder")
# test files are present
diagnose_test_files(path = fs::path(path_develcoder, "inst", "tinytest"))

# test directory is missing
diagnose_test_files(path = fs::path(path_develcoder, "inst", "othertest"))

# empty test directory
tempdir_example <- progutils::create_tempdir(prefix = "diagnose_test_files")
withr::with_dir(new = tempdir_example, {
  # Set up a package with testing infrastructure from tinytest
  desc <- desc::description$new("!new")
  desc$write(file = fs::path(tempdir_example, "DESCRIPTION"))
  desc::desc_set(Package = "somepkg")

  fs::dir_create(fs::path(tempdir_example, "inst", "tinytest"))
  diagnose_test_files(path = fs::path(tempdir_example, "inst", "tinytest"))
})

}
\seealso{
Other functions to check tests:
\code{\link[=check_test_files]{check_test_files()}},
\code{\link[=diagnose_test_infra]{diagnose_test_infra()}}
}
\concept{functions to check tests}
