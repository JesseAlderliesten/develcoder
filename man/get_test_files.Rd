% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_test_files.R
\name{get_test_files}
\alias{get_test_files}
\title{Get test files}
\usage{
get_test_files(
  testdir,
  pattern = "^test_|^test-",
  ignore_case = TRUE,
  signal = c("error", "warning", "message", "quiet")
)
}
\arguments{
\item{testdir}{\link[checkinput:is_character]{character string} containing a
\link[checkinput:is_path]{valid path} to the directory containing tests.}

\item{pattern}{\link[checkinput:is_character]{character string} containing a
\link[=regex]{regular expression} used to select test files.}

\item{ignore_case}{\code{TRUE} or \code{FALSE}: ignore case when matching filenames to
\code{pattern}?}

\item{signal}{\link[checkinput:is_character]{character string} indicating the
type of signal to be used:
\code{"error"} to throw an \link[=stop]{error}, \code{"warning"} to issue a \link{warning},
\code{"message"} to show a \link{message}, or \code{"quiet"} to be quiet.}
}
\value{
A character vector containing the found test files, which is \code{character(0)}
if no test files are found, with \link[=attributes]{attribute} \code{"info"} containing
a character string with the text of the \link[progutils:signal_text]{signal}, which is
\code{""} if no signal is emitted.
}
\description{
Get test files, emitting a signal if test files do not have corresponding
function files or vice versa.
}
\details{
The \link[progutils:signal_text]{signal} indicated by argument \code{signal} is emitted if
directory \code{testdir}:
\itemize{
\item does not \link[fs:dir_exists]{exist}
\item contains files that are ignored because they do not start with pattern
\code{pattern}, are not \R files, or are template files created by
\href{https://CRAN.R-project.org/package=tinytest}{\code{tinytest}}
\item does not contain any test files
}

A character string with the text of this signal is also present as attribute
\code{"info"} of the returned value.
}
\examples{
get_test_files(testdir = fs::path_wd("inst", "tinytest"), signal = "warning")

try(get_test_files(testdir = fs::path_wd("inst", "othertest"), signal = "warning"))

}
\seealso{
Other functions to check tests:
\code{\link[=check_test_files]{check_test_files()}},
\code{\link[=check_test_infra]{check_test_infra()}}
}
\concept{functions to check tests}
